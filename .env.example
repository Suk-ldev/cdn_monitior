# Debug Mode
DEBUG=false

# Note: API keys and other configuration will be read from environment variables during deployment
# For EdgeOne, set EO_SECRET_ID and EO_SECRET_KEY as environment variables
# For Cloudflare, set CF_TOKENS and CF_ZONES as environment variables
# For optional site configuration, set SITE_NAME and SITE_ICON as environment variables
